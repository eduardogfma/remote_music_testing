const library = require("./library");

// Routes
// - library
//      L library/update
//      L library/overview (genres, file types)
// - artists
// - artist/:id
// - albums
// - album/:id

module.exports = {
  library,
};
